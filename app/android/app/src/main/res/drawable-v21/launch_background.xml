<?xml version="1.0" encoding="utf-8"?>
<!-- Window background while the Flutter engine starts: the same "paper"
     the first Flutter frame paints, so the hand-off is invisible. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/dokki_paper" />
</layer-list>

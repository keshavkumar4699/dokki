name: vault_pdf
description: >-
  PdfComposer implementation for the dokki vault. The PDF library sits
  behind a five-method port; swapping it touches nothing else.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.10.0
resolution: workspace

dependencies:
  pdf: ^3.11.0
  vault_domain:
    path: ../vault_domain

dev_dependencies:
  test: ^1.26.3

name: vault_app_core
description: >-
  Application layer of the dokki vault: use cases, sessions, orchestration,
  and transaction scripts. Depends on vault_domain only.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.10.0
resolution: workspace

dependencies:
  collection: ^1.19.1
  vault_domain:
    path: ../vault_domain

dev_dependencies:
  fake_async: ^1.3.3
  test: ^1.26.3
